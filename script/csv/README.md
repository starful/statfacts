# Topic CSV (moved)

Content topics are managed in **okadmin** `data/topic_banks/okstats/`.
The Work Hub pipeline sets `TOPIC_QUEUE_*` and `TOPIC_BANK_*` when generating content.

Do not add topic rows in this directory.
